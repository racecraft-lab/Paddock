# API contract parity baseline ignore list
# One operation per line: METHOD /api/path
# Keep this list shrinking over time; remove entries when route/spec parity is fixed.
# PR #487 added these routes without OpenAPI specs — TODO: add specs and remove from ignore
GET /api/v1/evals/leaderboard
GET /api/v1/runs
GET /api/v1/runs/stream
GET /api/v1/runs/{run_id}
GET /api/v1/runs/{run_id}/provenance
PATCH /api/v1/runs/{run_id}
POST /api/v1/runs
PUT /api/v1/runs/{run_id}/eval
# PR #552 (gateway control panel) + #550 (hermes multi-agent) — TODO: add OpenAPI specs
GET /api/gateways/control
POST /api/gateways/control
GET /api/mcp-audit/verify
POST /api/hermes/events
# SPEC-008 test-mode-only fixture endpoints — gated behind MISSION_CONTROL_TEST_MODE=1
# AND admin auth, unreachable in prod, intentionally omitted from openapi.json.
DELETE /api/admin/spec-008/seed-fixture/{id}
POST /api/admin/spec-008/aegis-state
POST /api/admin/spec-008/breaker-state
POST /api/admin/spec-008/budget-utilization
POST /api/admin/spec-008/bulk-promote-state
POST /api/admin/spec-008/calibration-state
POST /api/admin/spec-008/emit-decision
POST /api/admin/spec-008/emit-dispatch
POST /api/admin/spec-008/override-grant-state
POST /api/admin/spec-008/seed-fixture
POST /api/admin/spec-008/seed-state
POST /api/admin/workspaces/{id}/feature-flags
