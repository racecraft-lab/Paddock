# State Machine and Feature Flags Checklist: SPEC-005 ready_for_owner State and Two-Step Terminal Event

**Purpose**: Validate state-machine and feature-flag requirement quality for SPEC-005 before task generation.
**Created**: 2026-05-02
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Requirement Completeness

- [ ] CHK001 Are flag-OFF requirements complete for preserving current `quality_review` to `done` behavior for both PR-producing and non-PR-producing tasks? [Completeness, Spec §US1, Spec §FR-004]
- [ ] CHK002 Are flag-OFF requirements complete for keeping existing `ready_for_owner` rows readable and visible while blocking new writes into that state? [Completeness, Spec §US1, Spec §FR-002, Spec §FR-003]
- [ ] CHK003 Are flag-ON routing requirements complete for sending PR-producing approvals to `ready_for_owner` while preserving direct `done` completion for non-PR-producing tasks? [Completeness, Spec §US2, Spec §FR-005, Spec §FR-006]
- [ ] CHK004 Are all named `done` transition sites covered by the requirements and plan, including Aegis dispatch, quality-review API, bulk task update, detail task update, failed-to-done recovery, and GitHub sync? [Coverage, Spec §US3, Spec §FR-010, Plan §Transition Sites]

## Requirement Clarity

- [ ] CHK005 Is the blocked completion conflict body specified with an exact status code, error string, reason string, and `task_ids` array shape for both single-task and multi-task routes? [Clarity, Spec §FR-011]
- [ ] CHK006 Is "verified merged PR evidence" defined with exact accepted evidence fields and explicit exclusions for closed issues, closed PRs, branch metadata, and issue timeline inference? [Clarity, Spec §FR-008, Spec §FR-009, Spec §FR-012, Spec §FR-013]
- [ ] CHK007 Is the PR-producing classification source clearly tied to the existing workflow-template fields and canonical `external_terminal_event='github_pr_merged'` value without adding schema scope? [Clarity, Spec §FR-024, Plan §Technical Context]
- [ ] CHK008 Is the `ready_for_owner` static vocabulary requirement clearly separated from flag-aware transition/write gating so reads, filters, schemas, stores, UI display, and GitHub labels remain unambiguous? [Clarity, Spec §FR-001, Spec §FR-003]

## Requirement Consistency

- [ ] CHK009 Are flag-resolution requirements consistent between the spec, plan, and constitution by requiring per-workspace `resolveFlag` decisions at every transition site and forbidding inline environment checks? [Consistency, Spec §FR-007, Plan §Constitution Check, Constitution §V]
- [ ] CHK010 Are no-migration constraints consistent across the spec and plan, including no database migration, DB CHECK, enum constraint, terminal-event table, issue timeline inference, or force-complete override? [Consistency, Spec §FR-024, Plan §Technical Context]
- [ ] CHK011 Are `ready_for_owner` and `awaiting_owner` semantics consistently defined as separate states with distinct owner-merge versus manual-blocked meaning? [Consistency, Spec §FR-019, Spec §FR-020]

## Scenario Coverage

- [ ] CHK012 Are blocked non-merge completion scenarios specified as side-effect-free across task status, activity, notifications, labels, and chain state? [Coverage, Spec §FR-010, Spec §FR-011, Spec §SC-003]
- [ ] CHK013 Are closed-issue-without-merged-PR reconciliation requirements complete for keeping the task in `ready_for_owner`, writing one deduped activity, notifying the right recipient, and avoiding duplicate noise? [Coverage, Spec §FR-014, Spec §FR-015, Spec §FR-016, Spec §FR-016a]
- [ ] CHK014 Are missing explicit PR linkage requirements complete for allowing entry into `ready_for_owner` while preventing completion until linkage and merged PR evidence exist? [Coverage, Spec §US2, Spec §Edge Cases]

## Acceptance Criteria Quality

- [ ] CHK015 Can every state-machine success criterion be objectively measured with 100% coverage targets for flag-ON routing, non-PR completion, blocked conflicts, verified merge completion, reconciliation, and operator visibility? [Measurability, Spec §SC-001..SC-006]
- [ ] CHK016 Is `advanceTaskChain` timing measurable and consistent: no advancement at `ready_for_owner`, advancement only after verified PR merge successfully writes `done`, and trigger identity recorded as the GitHub PR merge terminal event? [Measurability, Spec §FR-021, Plan §Shared Transition Boundary]

## Dependencies & Assumptions

- [ ] CHK017 Are dependencies on SPEC-004's PR-producing template flag and existing task status storage documented as assumptions rather than new SPEC-005 schema work? [Dependency, Spec §Assumptions, Plan §Technical Context]
- [ ] CHK018 Are test-fixture requirements for GitHub reconciliation clearly scoped to deterministic tests while production callsites preserve live GitHub behavior with no fixture/options? [Dependency, Spec §FR-022, Plan §GitHub Terminal Event]
