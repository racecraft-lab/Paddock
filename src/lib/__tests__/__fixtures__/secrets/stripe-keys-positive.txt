# Placeholder. The stripe-keys positive fixture is runtime-assembled in
# secret-detector.test.ts (RUNTIME_FIXTURES) to keep secret-shaped literals
# off disk, evading GitHub push-protection. The detector test still asserts
# regex match against the assembled string at runtime.
