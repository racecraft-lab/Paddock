{
  "name": "Task pipeline schema corpus",
  "purpose": "Fixed corpus for output-schema validator bounds, pattern safety, cache, and performance tests.",
  "cases": [
    "compact-object",
    "bounded-pattern",
    "oversized-schema",
    "oversized-output",
    "unsafe-pattern",
    "cache-lru"
  ]
}
