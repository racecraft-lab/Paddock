/**
 * SPEC-008 — Copilot schema versioning (T271). Per FR-090d + FR-090d1.
 */
import { describe, it } from 'vitest';

describe('SPEC-008 copilot schema versioning (T271)', () => {
  it.todo('known schema version accepted');
  it.todo('unknown schema version threshold reached → quarantine');
});
